import 'package:flutter/material.dart';

class SongAlbumsTab extends StatelessWidget {
  const SongAlbumsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [

      ],
    );
  }
}